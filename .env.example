# Optional: enable LLM-backed replies. Without these, the app uses its built-in
# offline response engine and works fully end-to-end with no external services.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Optional: override the API server port (defaults to 3001).
PORT=3001
